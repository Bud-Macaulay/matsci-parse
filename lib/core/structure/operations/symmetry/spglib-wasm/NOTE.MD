### NOTE

This is a handrolled wasm build of spglib-C. It was used for the old implementation of symmetry detection but has now been dropped in favour of spglib-moyo (and a hand rolled Niggli reduction algorithm)

It should remain here for benchmarking reasons (in which Niggli reduction is currently 1.5x faster than the TS implementation).
